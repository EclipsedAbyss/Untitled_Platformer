using UnityEngine;

public class LevelTimeRequirements : MonoBehaviour
{
    //this contains the times needed for each rank above D per level.these only exist to motivate players, andd are not to be used as punishment.
    //only as a reward
    public float timeForS;
    public float timeForA;
    public float timeForB;
    public float timeForC;
}
