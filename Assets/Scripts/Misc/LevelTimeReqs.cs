using UnityEngine;

public class LevelTimeReqs : MonoBehaviour
{
    //this is used to tell the timer whatathe times needed for each rank are in a level. do not use this as a punishment as it IS optional.
    public float timeForS;
    public float timeForA;
    public float timeForB;
    public float timeForC;
}
